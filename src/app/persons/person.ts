/* Defines the person entity */
export interface Person {
  id: number | null;
  personContId: string;
  personPID: string;
  externId: string;
  contractId: string;
  contactMean: string;
  name: string;
  firstName: string;
  state: string;
}
